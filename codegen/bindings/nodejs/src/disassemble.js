/**
 * MIDI file disassembler.
 * @module midiasm
 */

  /**
   * Parses a MIDI file byte array into an object.
   *
   * @param {bytearray} MIDI  MIDI file as a byte array.
   *
   * @example
   * midiasm.disassemble(bytes)
   *  .then(object => { console.log(object) })
   *  .catch(err => { console.log(`${err.message}`)
   */
export function disassemble(bytes) {
    throw new Error(`** NOT IMPLEMENTED ${bytes}`)
  }
